<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:title="Vibrate on move"
        android:summary="Do vibrate on successful move"
        android:key="vibrate"
        android:defaultValue="false"/>

    <CheckBoxPreference
        android:title="Sound on/off"
        android:summary="Have game tunes on while playing"
        android:key="sound"
        android:defaultValue="true"/>

    <ListPreference
        android:title="Number of points"
        android:summary="Set number of points on board"
        android:key="dotsCount"
        android:defaultValue="6"
        android:entries="@array/listArray"
        android:entryValues="@array/listValues"/>

    <Preference
        android:title="Reset High Score"
        android:summary="Clear the contents of the high score"
        android:key="resetHighScore" />


</PreferenceScreen>